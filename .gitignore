node_modules/
dist/
src-tauri/target/
# Tauri's build script regenerates gen/schemas on every cargo build — keep it
# ignored, but NOT gen/android (created once by `tauri android init` in Phase
# A0 and then hand-edited/committed — see PLAN-ANDROID.md §7).
src-tauri/gen/schemas/
*.local
.DS_Store
